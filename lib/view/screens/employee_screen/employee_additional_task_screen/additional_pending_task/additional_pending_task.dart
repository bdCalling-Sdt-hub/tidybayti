import 'package:flutter/material.dart';

class AdditionalPendingTask extends StatelessWidget {
  const AdditionalPendingTask({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('klklk')
      ],
    );
  }
}
