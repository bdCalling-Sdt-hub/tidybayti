import 'package:flutter/material.dart';

class AdditionalCompletedTask extends StatelessWidget {
  const AdditionalCompletedTask({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('klklk')
      ],
    );
  }
}
