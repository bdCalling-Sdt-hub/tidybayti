import 'package:flutter/material.dart';

class HomeOwnerChose extends StatelessWidget {
  const HomeOwnerChose({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
